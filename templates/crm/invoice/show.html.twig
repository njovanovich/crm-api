{% extends 'base.html.twig' %}

{% block title %}Invoice{% endblock %}

{% block body %}
    <h1>Invoice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invoice.id }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ invoice.created ? invoice.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ invoice.updated ? invoice.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>InvoiceLines</th>
                <td>{{ invoice.invoiceLines }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crm_invoice_index') }}">back to list</a>

    <a href="{{ path('crm_invoice_edit', {'id': invoice.id}) }}">edit</a>

    {{ include('crm_invoice/_delete_form.html.twig') }}
{% endblock %}
