{% extends 'base.html.twig' %}

{% block title %}Invoice index{% endblock %}

{% block body %}
    <h1>Invoice index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created</th>
                <th>Updated</th>
                <th>InvoiceLines</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for invoice in invoices %}
            <tr>
                <td>{{ invoice.id }}</td>
                <td>{{ invoice.created ? invoice.created|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ invoice.updated ? invoice.updated|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ invoice.invoiceLines }}</td>
                <td>
                    <a href="{{ path('crm_invoice_show', {'id': invoice.id}) }}">show</a>
                    <a href="{{ path('crm_invoice_edit', {'id': invoice.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('crm_invoice_new') }}">Create new</a>
{% endblock %}
