{% extends 'base.html.twig' %}

{% block title %}Edit Invoice{% endblock %}

{% block body %}
    <h1>Edit Invoice</h1>

    {{ include('crm_invoice/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('crm_invoice_index') }}">back to list</a>

    {{ include('crm_invoice/_delete_form.html.twig') }}
{% endblock %}
