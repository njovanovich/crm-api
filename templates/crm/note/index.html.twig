{% extends 'base.html.twig' %}

{% block title %}Note index{% endblock %}

{% block body %}
    <h1>Note index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created</th>
                <th>Updated</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for note in notes %}
            <tr>
                <td>{{ note.id }}</td>
                <td>{{ note.created ? note.created|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ note.updated ? note.updated|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('crm_note_show', {'id': note.id}) }}">show</a>
                    <a href="{{ path('crm_note_edit', {'id': note.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('crm_note_new') }}">Create new</a>
{% endblock %}
