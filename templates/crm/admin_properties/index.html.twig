{% extends 'base.html.twig' %}

{% block title %}AdminProperties index{% endblock %}

{% block body %}
    <h1>AdminProperties index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created</th>
                <th>Updated</th>
                <th>Name</th>
                <th>Value</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for admin_property in admin_properties %}
            <tr>
                <td>{{ admin_property.id }}</td>
                <td>{{ admin_property.created ? admin_property.created|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ admin_property.updated ? admin_property.updated|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ admin_property.name }}</td>
                <td>{{ admin_property.value }}</td>
                <td>
                    <a href="{{ path('crm_admin_properties_show', {'id': admin_property.id}) }}">show</a>
                    <a href="{{ path('crm_admin_properties_edit', {'id': admin_property.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('crm_admin_properties_new') }}">Create new</a>
{% endblock %}
